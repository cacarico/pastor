# Copy to docs/demo/local/flock.toml. The recording shows these names and
# hosts as written, so use ssh aliases rather than addresses.
[[machine]]
name = "pi-1"
ssh = "pi-1"
max_agents = 3
tags = ["fast"]

[[machine]]
name = "pi-2"
ssh = "pi-2"
max_agents = 3
