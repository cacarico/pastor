# Ledger: nested-herdr

Plan: `docs/superpowers/plans/2026-09-26-nested-herdr.md`, branch `pastor/nested-herdr`.
One line per event, appended, never edited. The next task is the first with no `complete` line.

