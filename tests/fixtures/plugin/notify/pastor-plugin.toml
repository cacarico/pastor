# Test fixture: hooks only, for every task's end (not only_own).
id = "notify"
version = "0.1.0"
description = "Fixture: records every task.done it hears"

[[events]]
on = ["task.done"]
command = ["sh", "notify.sh"]
